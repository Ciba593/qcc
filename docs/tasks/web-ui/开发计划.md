# QCC Web UI 开发计划

## 📋 项目概述

为 QCC (Quick Claude Config) 开发一个现代化的 Web 界面,将所有 CLI 功能映射到可视化的 UI 中,提供更友好的用户体验。

**目标**: 实现所有 CLI 命令的 Web UI 对应功能,让用户可以通过浏览器完成所有配置管理和监控操作。

---

## 🎯 功能覆盖范围

### 核心功能模块

1. **配置管理** - 对应 CLI: `add`, `list`, `use`, `default`, `remove`, `sync`
2. **Endpoint 管理** - 对应 CLI: `endpoint add/list/remove`
3. **优先级管理** - 对应 CLI: `priority set/list/switch/history/policy`
4. **代理服务** - 对应 CLI: `proxy start/stop/status/use/logs`
5. **健康监控** - 对应 CLI: `health test/metrics/status/history/config/check`
6. **失败队列** - 对应 CLI: `queue status/list/retry/retry-all/clear`
7. **厂商配置** - 对应 CLI: `fc`
8. **系统管理** - 对应 CLI: `init`, `config`, `status`, `uninstall`

---

## 🏗️ 技术架构

### 前端技术栈

```
框架: React 18 + TypeScript
UI 库: Ant Design / Shadcn UI
状态管理: Zustand / Redux Toolkit
路由: React Router v6
数据请求: TanStack Query (React Query)
图表: ECharts / Recharts
实时通信: WebSocket / Socket.IO
构建工具: Vite
```

### 后端技术栈

```
框架: FastAPI (Python)
WebSocket: FastAPI WebSocket 支持
CORS: FastAPI CORS 中间件
数据验证: Pydantic
异步支持: asyncio + aiohttp
集成: 复用现有 fastcc 模块
```

### 部署方式

```
模式 1: 嵌入式 Web 服务 (推荐)
- 通过 qcc web start 启动 Web 服务
- 监听 http://127.0.0.1:8080
- 自动打开浏览器

模式 2: 独立部署
- 前端静态文件部署到 CDN
- 后端 API 独立部署
```

---

## 📐 页面结构设计

### 主布局

```
┌────────────────────────────────────────────┐
│  顶部导航栏 (TopNav)                         │
│  [QCC Logo] [状态指示器] [用户菜单]          │
├──────────┬─────────────────────────────────┤
│          │                                  │
│  侧边栏   │  主内容区                         │
│  (Sider) │  (Content)                       │
│          │                                  │
│  - 仪表盘 │  [当前页面内容]                   │
│  - 配置   │                                  │
│  - 端点   │                                  │
│  - 优先级 │                                  │
│  - 健康   │                                  │
│  - 代理   │                                  │
│  - 指标   │                                  │
│  - 队列   │                                  │
│  - 设置   │                                  │
│          │                                  │
└──────────┴─────────────────────────────────┘
```

### 页面清单

| 页面路径 | 页面名称 | 对应 CLI 命令 | 优先级 |
|---------|---------|--------------|-------|
| `/` | 仪表盘 | `status` | P0 |
| `/configs` | 配置管理 | `add/list/use/default/remove` | P0 |
| `/endpoints` | Endpoint 管理 | `endpoint add/list/remove` | P0 |
| `/priority` | 优先级管理 | `priority set/list/switch/history/policy` | P1 |
| `/health` | 健康监控 | `health test/metrics/status/history/config` | P1 |
| `/proxy` | 代理服务 | `proxy start/stop/status/use/logs` | P0 |
| `/metrics` | 性能指标 | `health metrics` | P2 |
| `/queue` | 失败队列 | `queue status/list/retry/retry-all/clear` | P1 |
| `/settings` | 系统设置 | `config/init/uninstall` | P2 |
| `/providers` | 厂商配置 | `fc` | P1 |

---

## 🚀 实施路线图

### Phase 1: 基础设施搭建 (第 1-2 周)

#### 1.1 项目初始化

**任务清单**:
- [ ] 创建前端项目结构 (`qcc-web/`)
  ```bash
  npm create vite@latest qcc-web -- --template react-ts
  ```
- [ ] 安装核心依赖
  ```bash
  npm install antd @ant-design/icons
  npm install react-router-dom
  npm install @tanstack/react-query
  npm install zustand
  npm install echarts echarts-for-react
  npm install socket.io-client
  npm install axios
  ```
- [ ] 配置 TypeScript 和 ESLint
- [ ] 设置项目目录结构

**目录结构**:
```
qcc-web/
├── src/
│   ├── api/              # API 接口
│   ├── components/       # 通用组件
│   ├── pages/            # 页面组件
│   ├── hooks/            # 自定义 Hooks
│   ├── store/            # 状态管理
│   ├── types/            # TypeScript 类型
│   ├── utils/            # 工具函数
│   ├── App.tsx
│   └── main.tsx
├── public/
└── package.json
```

#### 1.2 后端 API 开发

**任务清单**:
- [ ] 创建 FastAPI 应用 (`fastcc/web/app.py`)
- [ ] 实现 CORS 配置
- [ ] 设置路由结构
- [ ] 实现 WebSocket 支持
- [ ] 添加 API 文档 (Swagger)

**文件结构**:
```
fastcc/web/
├── __init__.py
├── app.py              # FastAPI 主应用
├── routers/            # API 路由
│   ├── __init__.py
│   ├── configs.py      # 配置管理
│   ├── endpoints.py    # Endpoint 管理
│   ├── priority.py     # 优先级管理
│   ├── health.py       # 健康监控
│   ├── proxy.py        # 代理服务
│   └── queue.py        # 失败队列
├── websocket.py        # WebSocket 处理
└── models.py           # Pydantic 模型
```

#### 1.3 CLI 集成

**任务清单**:
- [ ] 在 `cli.py` 添加 `web` 命令组
  ```bash
  qcc web start    # 启动 Web 服务
  qcc web stop     # 停止 Web 服务
  qcc web status   # 查看 Web 服务状态
  ```
- [ ] 实现自动打开浏览器功能
- [ ] 配置静态文件服务

---

### Phase 2: 核心功能实现 (第 3-5 周)

#### 2.1 仪表盘页面 (第 3 周)

**功能点**:
- [ ] 系统状态概览卡片
  - 配置总数、活跃配置、存储方式
  - 代理服务状态 (运行/停止)
  - Endpoint 健康分布
- [ ] 实时状态更新 (WebSocket)
- [ ] 快速操作按钮
  - 启动 Claude Code
  - 同步配置
  - 查看日志
- [ ] 最近活动日志列表

**API 接口**:
```typescript
GET /api/dashboard/summary    // 获取概览数据
GET /api/dashboard/activity   // 获取活动日志
WS  /ws/dashboard            // 实时状态推送
```

**组件开发**:
- `StatCard.tsx` - 统计卡片
- `ActivityLog.tsx` - 活动日志
- `QuickActions.tsx` - 快速操作

#### 2.2 配置管理页面 (第 3 周)

**功能点**:
- [ ] 配置列表展示 (表格/卡片视图)
- [ ] 添加配置对话框
- [ ] 编辑配置功能
- [ ] 删除配置 (带确认)
- [ ] 设置默认配置
- [ ] 使用配置启动 Claude Code
- [ ] 搜索和过滤
- [ ] 配置导入/导出

**API 接口**:
```typescript
GET    /api/configs           // 获取所有配置
POST   /api/configs           // 创建配置
GET    /api/configs/:id       // 获取单个配置
PUT    /api/configs/:id       // 更新配置
DELETE /api/configs/:id       // 删除配置
POST   /api/configs/:id/use   // 使用配置
POST   /api/configs/:id/default // 设置默认
POST   /api/configs/sync      // 手动同步
```

**组件开发**:
- `ConfigList.tsx` - 配置列表
- `ConfigCard.tsx` - 配置卡片
- `ConfigForm.tsx` - 配置表单
- `ConfigActions.tsx` - 操作按钮组

#### 2.3 Endpoint 管理页面 (第 4 周)

**功能点**:
- [ ] Endpoint 列表展示
- [ ] 按配置分组显示
- [ ] 创建 Endpoint 集群向导
- [ ] 添加/编辑/删除 Endpoint
- [ ] 健康状态实时显示
- [ ] 权重和优先级调整
- [ ] 拖拽排序功能
- [ ] Endpoint 测试功能

**API 接口**:
```typescript
GET    /api/endpoints                    // 获取所有 endpoint
POST   /api/endpoints                    // 创建 endpoint
GET    /api/endpoints/:id                // 获取详情
PUT    /api/endpoints/:id                // 更新 endpoint
DELETE /api/endpoints/:id                // 删除 endpoint
POST   /api/endpoints/:id/test           // 测试 endpoint
GET    /api/endpoints/config/:configId   // 按配置获取
```

**组件开发**:
- `EndpointList.tsx` - Endpoint 列表
- `EndpointCard.tsx` - Endpoint 卡片
- `EndpointForm.tsx` - Endpoint 表单
- `EndpointClusterWizard.tsx` - 集群创建向导
- `HealthBadge.tsx` - 健康状态徽章

#### 2.4 代理服务页面 (第 4 周)

**功能点**:
- [ ] 代理服务状态监控
- [ ] 启动/停止/重启控制
- [ ] 配置参数调整
  - 监听地址/端口
  - 集群配置选择
  - 负载均衡策略
- [ ] 实时日志流显示
- [ ] 日志过滤和搜索
- [ ] 日志下载功能

**API 接口**:
```typescript
GET    /api/proxy/status       // 获取代理状态
POST   /api/proxy/start        // 启动代理
POST   /api/proxy/stop         // 停止代理
POST   /api/proxy/restart      // 重启代理
GET    /api/proxy/logs         // 获取日志
WS     /ws/proxy/logs          // 实时日志流
PUT    /api/proxy/config       // 更新配置
```

**组件开发**:
- `ProxyControl.tsx` - 控制面板
- `ProxyConfig.tsx` - 配置表单
- `ProxyLogs.tsx` - 日志显示
- `LogViewer.tsx` - 日志查看器

#### 2.5 厂商快速配置页面 (第 5 周)

**功能点**:
- [ ] 厂商列表展示
- [ ] 厂商筛选和搜索
- [ ] 配置向导流程
  1. 选择厂商
  2. 浏览器注册引导
  3. 输入 API Key
  4. 验证配置
  5. 保存并启动
- [ ] API Key 验证
- [ ] 配置预览
- [ ] 一键应用

**API 接口**:
```typescript
GET    /api/providers          // 获取厂商列表
POST   /api/providers/validate // 验证 API Key
POST   /api/providers/quick-config // 快速配置
```

**组件开发**:
- `ProviderList.tsx` - 厂商列表
- `ProviderCard.tsx` - 厂商卡片
- `QuickConfigWizard.tsx` - 配置向导
- `ApiKeyInput.tsx` - API Key 输入

---

### Phase 3: 高级功能实现 (第 6-8 周)

#### 3.1 优先级管理页面 (第 6 周)

**功能点**:
- [ ] 三级优先级可视化
  - Primary (主配置)
  - Secondary (次配置)
  - Fallback (兜底配置)
- [ ] 拖拽分配配置到各级别
- [ ] 手动切换配置
- [ ] 切换历史时间线
- [ ] 故障转移策略配置
  - 自动故障转移开关
  - 自动恢复开关
  - 故障阈值设置
  - 冷却期设置

**API 接口**:
```typescript
GET    /api/priority                  // 获取优先级配置
PUT    /api/priority/:level           // 设置优先级
POST   /api/priority/switch/:profile  // 手动切换
GET    /api/priority/history          // 切换历史
PUT    /api/priority/policy           // 更新策略
```

**组件开发**:
- `PriorityLevels.tsx` - 优先级层次
- `PriorityCard.tsx` - 优先级卡片
- `PriorityTimeline.tsx` - 切换时间线
- `FailoverPolicyForm.tsx` - 策略配置表单
- `DragDropPriority.tsx` - 拖拽组件

#### 3.2 健康监控页面 (第 6-7 周)

**功能点**:
- [ ] 健康状态看板
  - 健康/警告/失败 统计
  - Endpoint 健康列表
  - 状态颜色编码
- [ ] 对话式健康测试
  - 执行测试按钮
  - 测试进度显示
  - 测试结果展示
- [ ] 测试历史记录
- [ ] 健康检查配置
  - 检查间隔设置
  - 权重调整开关
  - 最小检查次数
- [ ] 自动刷新控制

**API 接口**:
```typescript
GET    /api/health/status             // 健康状态
POST   /api/health/test               // 执行测试
POST   /api/health/test/:endpointId   // 测试单个 endpoint
GET    /api/health/history/:id        // 历史记录
PUT    /api/health/config             // 更新配置
WS     /ws/health                     // 实时状态推送
```

**组件开发**:
- `HealthDashboard.tsx` - 健康看板
- `HealthStatusCard.tsx` - 状态卡片
- `HealthTestDialog.tsx` - 测试对话框
- `HealthHistory.tsx` - 历史记录
- `HealthConfigForm.tsx` - 配置表单

#### 3.3 性能指标页面 (第 7 周)

**功能点**:
- [ ] 关键指标展示
  - 总请求数
  - 成功率
  - 平均响应时间
  - P95/P99 延迟
- [ ] 响应时间趋势图 (折线图)
- [ ] 成功率分布图 (柱状图)
- [ ] Endpoint 性能对比 (雷达图)
- [ ] 时间范围选择器
- [ ] Endpoint 筛选器
- [ ] 数据导出功能 (CSV/JSON)

**API 接口**:
```typescript
GET /api/metrics/summary              // 指标汇总
GET /api/metrics/timeseries          // 时间序列数据
GET /api/metrics/endpoint/:id        // 单个 endpoint 指标
GET /api/metrics/compare             // 对比数据
POST /api/metrics/export             // 导出数据
```

**组件开发**:
- `MetricsSummary.tsx` - 指标汇总
- `ResponseTimeChart.tsx` - 响应时间图表
- `SuccessRateChart.tsx` - 成功率图表
- `RadarCompare.tsx` - 雷达对比图
- `TimeRangeSelector.tsx` - 时间选择器
- `MetricsExport.tsx` - 导出功能

#### 3.4 失败队列页面 (第 8 周)

**功能点**:
- [ ] 队列统计概览
  - 队列大小
  - 待重试数量
  - 成功率
- [ ] 失败请求列表
  - 请求 ID
  - 失败原因
  - 重试次数
  - 入队时间
  - 下次重试时间
- [ ] 批量操作
  - 重试所有
  - 清空队列
- [ ] 单个请求操作
  - 立即重试
  - 查看详情
  - 移除
- [ ] 请求详情对话框

**API 接口**:
```typescript
GET    /api/queue/status       // 队列状态
GET    /api/queue/items        // 队列列表
POST   /api/queue/retry/:id    // 重试请求
POST   /api/queue/retry-all    // 重试所有
DELETE /api/queue/clear        // 清空队列
GET    /api/queue/item/:id     // 请求详情
```

**组件开发**:
- `QueueSummary.tsx` - 队列统计
- `QueueList.tsx` - 队列列表
- `QueueItem.tsx` - 队列项
- `QueueActions.tsx` - 批量操作
- `RequestDetail.tsx` - 请求详情

---

### Phase 4: 完善与优化 (第 9-10 周)

#### 4.1 系统设置页面 (第 9 周)

**功能点**:
- [ ] 通用设置
  - 默认配置选择
  - 自动同步开关
  - 启动检查更新
  - 界面语言
  - 主题切换 (浅色/深色/自动)
- [ ] 存储设置
  - 存储方式选择
  - 加密存储开关
  - 缓存过期时间
  - 更改存储方式向导
- [ ] 代理设置
  - 默认监听地址
  - 默认监听端口
  - 负载均衡策略
  - 请求超时时间
- [ ] 高级选项
  - 调试模式
  - 日志级别
- [ ] 导入/导出设置
- [ ] 重置默认值

**API 接口**:
```typescript
GET    /api/settings           // 获取设置
PUT    /api/settings           // 更新设置
POST   /api/settings/import    // 导入设置
POST   /api/settings/export    // 导出设置
POST   /api/settings/reset     // 重置默认
```

**组件开发**:
- `SettingsTabs.tsx` - 设置标签页
- `GeneralSettings.tsx` - 通用设置
- `StorageSettings.tsx` - 存储设置
- `ProxySettings.tsx` - 代理设置
- `AdvancedSettings.tsx` - 高级设置

#### 4.2 响应式优化 (第 9 周)

**任务清单**:
- [ ] 移动端适配
  - 侧边栏折叠
  - 触摸手势支持
  - 底部导航栏 (移动端)
- [ ] 平板适配
  - 栅格布局调整
  - 卡片尺寸优化
- [ ] 断点设置
  - xs: < 576px
  - sm: ≥ 576px
  - md: ≥ 768px
  - lg: ≥ 992px
  - xl: ≥ 1200px
  - xxl: ≥ 1600px

#### 4.3 性能优化 (第 10 周)

**任务清单**:
- [ ] 代码分割 (React.lazy)
- [ ] 路由懒加载
- [ ] 图片懒加载
- [ ] 虚拟滚动 (大列表)
- [ ] 防抖和节流优化
- [ ] WebSocket 连接池
- [ ] 数据缓存策略 (React Query)
- [ ] 打包优化 (Vite)

#### 4.4 用户体验优化 (第 10 周)

**任务清单**:
- [ ] 加载状态指示器 (Skeleton/Spin)
- [ ] 错误边界处理
- [ ] Toast 通知系统
- [ ] 操作确认对话框
- [ ] 空状态页面
- [ ] 键盘快捷键
  - `Ctrl/Cmd + K`: 全局搜索
  - `Ctrl/Cmd + S`: 保存
  - `Esc`: 关闭对话框
- [ ] 国际化支持 (i18n)
- [ ] 暗黑模式

#### 4.5 测试与修复 (第 10 周)

**任务清单**:
- [ ] 单元测试 (Vitest)
  - 组件测试
  - 工具函数测试
- [ ] 集成测试
  - API 集成测试
  - WebSocket 测试
- [ ] E2E 测试 (Playwright)
  - 关键用户流程
- [ ] 浏览器兼容性测试
  - Chrome/Edge
  - Firefox
  - Safari
- [ ] 性能测试
  - Lighthouse 评分
  - 网络性能
- [ ] 安全测试
  - XSS 防护
  - CSRF 防护
- [ ] Bug 修复和优化

---

## 📊 功能与 CLI 映射表

### 完整映射清单

| CLI 命令 | Web UI 页面 | 功能描述 | 优先级 |
|---------|-----------|---------|-------|
| `qcc` (智能启动) | 仪表盘 | 快速操作按钮 | P0 |
| `qcc init` | 设置页面 | 初始化向导 | P0 |
| `qcc add <name>` | 配置管理 | 添加配置对话框 | P0 |
| `qcc list` | 配置管理 | 配置列表展示 | P0 |
| `qcc use [name]` | 配置管理 | 使用配置按钮 | P0 |
| `qcc default [name]` | 配置管理 | 设为默认按钮 | P0 |
| `qcc remove [name]` | 配置管理 | 删除配置操作 | P0 |
| `qcc sync` | 配置管理 | 手动同步按钮 | P0 |
| `qcc config` | 设置页面 | 配置管理菜单 | P0 |
| `qcc status` | 仪表盘 | 系统状态看板 | P0 |
| `qcc fc` | 厂商配置 | 快速配置向导 | P1 |
| `qcc proxy start` | 代理服务 | 启动按钮 | P0 |
| `qcc proxy stop` | 代理服务 | 停止按钮 | P0 |
| `qcc proxy status` | 代理服务 | 状态监控 | P0 |
| `qcc proxy use` | 代理服务 | 配置选择 | P0 |
| `qcc proxy logs` | 代理服务 | 日志查看器 | P0 |
| `qcc endpoint add` | Endpoint 管理 | 创建集群向导 | P0 |
| `qcc endpoint list` | Endpoint 管理 | Endpoint 列表 | P0 |
| `qcc endpoint remove` | Endpoint 管理 | 删除操作 | P0 |
| `qcc priority set` | 优先级管理 | 优先级设置 | P1 |
| `qcc priority list` | 优先级管理 | 优先级展示 | P1 |
| `qcc priority switch` | 优先级管理 | 手动切换 | P1 |
| `qcc priority history` | 优先级管理 | 切换历史 | P1 |
| `qcc priority policy` | 优先级管理 | 策略配置 | P1 |
| `qcc health test` | 健康监控 | 执行测试 | P1 |
| `qcc health metrics` | 性能指标 | 指标展示 | P1 |
| `qcc health status` | 健康监控 | 状态看板 | P1 |
| `qcc health history` | 健康监控 | 历史记录 | P1 |
| `qcc health config` | 健康监控 | 配置表单 | P1 |
| `qcc queue status` | 失败队列 | 队列统计 | P1 |
| `qcc queue list` | 失败队列 | 队列列表 | P1 |
| `qcc queue retry` | 失败队列 | 重试操作 | P1 |
| `qcc queue retry-all` | 失败队列 | 批量重试 | P1 |
| `qcc queue clear` | 失败队列 | 清空队列 | P1 |
| `qcc uninstall` | 设置页面 | 卸载功能 | P2 |

**优先级说明**:
- P0: 核心功能,必须实现
- P1: 重要功能,第一版本包含
- P2: 次要功能,可延后实现

---

## 🎨 设计规范

### 视觉设计

**配色方案**:
- 主色: `#1890ff` (Ant Design Blue)
- 成功: `#52c41a` (Green)
- 警告: `#faad14` (Orange)
- 错误: `#f5222d` (Red)
- 文本: `#000000d9` (85% Black)
- 背景: `#f0f2f5` (Light Gray)

**健康状态配色**:
- 健康 (Healthy): `#52c41a` (绿色)
- 警告 (Degraded): `#faad14` (橙色)
- 失败 (Unhealthy): `#f5222d` (红色)
- 未知 (Unknown): `#d9d9d9` (灰色)

**字体**:
- 主字体: `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'PingFang SC', 'Microsoft YaHei'`
- 代码字体: `'Fira Code', 'Source Code Pro', Consolas, monospace`

### 组件规范

**按钮**:
- 主要操作: `type="primary"`
- 危险操作: `danger` (带确认)
- 次要操作: `type="default"`
- 文本链接: `type="link"`

**表单**:
- 标签对齐: `labelCol={{ span: 6 }}`
- 输入框宽度: `wrapperCol={{ span: 18 }}`
- 必填标记: 红色星号

**卡片**:
- 圆角: `borderRadius: 8px`
- 阴影: `boxShadow: 0 2px 8px rgba(0,0,0,0.1)`
- 内边距: `padding: 24px`

**间距**:
- 小: `8px`
- 中: `16px`
- 大: `24px`
- 超大: `32px`

---

## 🔌 API 接口文档

### 基础接口

**请求格式**:
```typescript
// 请求头
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <token>" // 可选
}

// 响应格式
{
  "success": boolean,
  "data": any,
  "message": string,
  "timestamp": string
}

// 错误响应
{
  "success": false,
  "error": {
    "code": string,
    "message": string,
    "details": any
  },
  "timestamp": string
}
```

### WebSocket 消息格式

**订阅消息**:
```typescript
{
  "type": "subscribe",
  "channel": "proxy.status" | "health.updates" | "queue.updates",
  "params": {}
}
```

**推送消息**:
```typescript
{
  "type": "update",
  "channel": string,
  "data": any,
  "timestamp": string
}
```

### 核心 API 端点

详见各 Phase 中的接口定义。

---

## 🧪 测试策略

### 测试覆盖目标

- **单元测试**: 覆盖率 ≥ 70%
- **集成测试**: 核心流程 100%
- **E2E 测试**: 关键用户路径

### 测试用例

#### 前端测试

**组件测试**:
- [ ] 配置表单验证
- [ ] 按钮点击事件
- [ ] 列表渲染
- [ ] 状态更新

**集成测试**:
- [ ] API 调用流程
- [ ] WebSocket 连接
- [ ] 状态管理

#### 后端测试

**API 测试**:
- [ ] 配置 CRUD 操作
- [ ] Endpoint 管理
- [ ] 健康检查
- [ ] 代理控制

**WebSocket 测试**:
- [ ] 连接建立
- [ ] 消息推送
- [ ] 断线重连

#### E2E 测试场景

1. **配置管理流程**
   - 添加新配置
   - 编辑配置
   - 使用配置启动
   - 删除配置

2. **Endpoint 集群创建**
   - 创建集群
   - 添加 endpoint
   - 测试连接
   - 启动代理

3. **健康监控流程**
   - 查看健康状态
   - 执行对话测试
   - 查看测试结果
   - 查看历史记录

---

## 📦 部署方案

### 开发环境

```bash
# 前端开发服务器
cd qcc-web
npm run dev          # http://localhost:5173

# 后端开发服务器
uvx --from . qcc web start --dev
# API: http://127.0.0.1:8080
# Docs: http://127.0.0.1:8080/docs
```

### 生产部署

#### 方案 1: 嵌入式部署 (推荐)

```bash
# 1. 构建前端
cd qcc-web
npm run build
# 输出到 dist/

# 2. 复制静态文件到 Python 包
cp -r dist/* ../fastcc/web/static/

# 3. 启动服务 (包含静态文件服务)
qcc web start
# 访问 http://127.0.0.1:8080
```

**优点**:
- 一键启动,无需额外配置
- 与 CLI 工具集成
- 适合桌面应用场景

#### 方案 2: 分离部署

**前端**:
```bash
# 部署到 CDN/Nginx
cd qcc-web
npm run build
# 上传 dist/ 到服务器
```

**后端**:
```bash
# 使用 uvicorn 启动
uvicorn fastcc.web.app:app --host 0.0.0.0 --port 8080
```

**优点**:
- 前后端独立扩展
- 支持 CDN 加速
- 适合云端部署场景

---

## 🚧 风险与挑战

### 技术风险

1. **实时性能**
   - 风险: WebSocket 连接数过多导致性能下降
   - 应对: 实现连接池,限制并发连接数

2. **状态同步**
   - 风险: CLI 和 Web UI 状态不一致
   - 应对: 使用事件总线,实时同步状态变化

3. **浏览器兼容性**
   - 风险: 旧版浏览器不支持某些特性
   - 应对: Polyfill + 降级方案

### 业务风险

1. **功能覆盖**
   - 风险: 部分 CLI 功能难以映射到 UI
   - 应对: 提供高级模式/专家模式

2. **用户习惯**
   - 风险: 用户更习惯 CLI 操作
   - 应对: 保留 CLI,UI 作为补充

---

## 📈 成功指标

### 开发指标

- [ ] 所有 CLI 命令 100% 映射到 UI
- [ ] 单元测试覆盖率 ≥ 70%
- [ ] E2E 测试通过率 100%
- [ ] Lighthouse 性能评分 ≥ 90

### 用户体验指标

- [ ] 页面加载时间 < 2s
- [ ] 操作响应时间 < 500ms
- [ ] 实时数据延迟 < 1s
- [ ] 移动端可用性评分 ≥ 90

---

## 📝 文档计划

### 开发文档

- [ ] API 接口文档 (Swagger)
- [ ] 组件库文档 (Storybook)
- [ ] 架构设计文档
- [ ] 部署指南

### 用户文档

- [ ] 快速开始指南
- [ ] 功能使用教程
- [ ] 常见问题 FAQ
- [ ] 视频演示

---

## 🎯 里程碑

### M1: 基础设施 (第 2 周末)
- [x] 项目初始化完成
- [ ] 基础 API 框架完成
- [ ] 前端路由和布局完成

### M2: 核心功能 (第 5 周末)
- [ ] 仪表盘完成
- [ ] 配置管理完成
- [ ] Endpoint 管理完成
- [ ] 代理服务完成
- [ ] 厂商配置完成

### M3: 高级功能 (第 8 周末)
- [ ] 优先级管理完成
- [ ] 健康监控完成
- [ ] 性能指标完成
- [ ] 失败队列完成

### M4: 发布就绪 (第 10 周末)
- [ ] 系统设置完成
- [ ] 响应式优化完成
- [ ] 性能优化完成
- [ ] 测试完成
- [ ] 文档完成
- [ ] 正式发布 v1.0.0

---

## 🔄 迭代计划

### v1.0.0 (第 10 周)
- 所有 P0 和 P1 功能
- 基础文档

### v1.1.0 (第 12 周)
- P2 功能补充
- 性能优化
- 用户反馈改进

### v1.2.0 (第 14 周)
- 高级功能扩展
- 插件系统
- 多语言支持

---

## 📞 团队协作

### 角色分工

- **前端开发**: React/TypeScript 开发
- **后端开发**: FastAPI/Python 开发
- **UI/UX 设计**: 界面设计和交互
- **测试工程师**: 测试用例编写和执行
- **技术文档**: API 文档和用户手册

### 协作工具

- **代码管理**: Git + GitHub
- **项目管理**: GitHub Projects
- **设计协作**: Figma
- **文档协作**: Notion/飞书文档
- **沟通工具**: Slack/微信

---

## 📋 待办事项检查清单

### 立即开始

- [ ] 创建 `qcc-web/` 前端项目
- [ ] 创建 `fastcc/web/` 后端模块
- [ ] 在 `cli.py` 添加 `web` 命令组
- [ ] 设计数据库 Schema (如需要)
- [ ] 搭建 CI/CD 流程

### 本周完成

- [ ] 完成项目框架搭建
- [ ] 实现基础 API 路由
- [ ] 完成主布局组件
- [ ] 实现 WebSocket 连接

### 本月目标

- [ ] 完成 Phase 1 和 Phase 2
- [ ] 核心功能可用
- [ ] 内部测试版本

---

## 📚 参考资源

### 官方文档

- [React 文档](https://react.dev)
- [TypeScript 文档](https://www.typescriptlang.org/docs)
- [Ant Design](https://ant.design)
- [FastAPI 文档](https://fastapi.tiangolo.com)
- [Vite 文档](https://vitejs.dev)

### 社区资源

- [React Query](https://tanstack.com/query)
- [Zustand](https://docs.pmnd.rs/zustand)
- [ECharts](https://echarts.apache.org)
- [Socket.IO](https://socket.io)

---

## 🎉 总结

本开发计划涵盖了 QCC Web UI 的完整实施方案,包括:

✅ **功能覆盖**: 100% CLI 命令映射到 Web UI
✅ **技术选型**: 现代化技术栈,易于开发和维护
✅ **分阶段实施**: 10 周完成,4 个主要阶段
✅ **质量保证**: 完善的测试和文档计划
✅ **部署方案**: 灵活的部署选项

**下一步行动**:
1. 确认技术选型和架构设计
2. 开始 Phase 1 基础设施搭建
3. 建立团队协作流程
4. 启动开发工作

---

*本文档由 QCC 开发团队维护,最后更新: 2025-01-17*
